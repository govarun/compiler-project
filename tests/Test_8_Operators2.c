int main(){
    int a = 5, *b = &a , intt ;
    int d = b->a ;
    // pointer operator to be added
    int e = b.a ;
    if(true) // true and false needs to be added
    {
        int e = (a > intt) ? a : b ; 
    }
    return 0;
}
